[project]
name = "coding-agent-workspace"
version = "0"
description = "uv workspace root: agent-harness engine, its TUI frontend, and the coding domain."
requires-python = ">=3.12,<3.13"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/agent_harness", "packages/tui", "coding"]
